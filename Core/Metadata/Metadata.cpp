#include "Core/Metadata/Metadata.h"

namespace Core::Metadata {

// Stub implementations for MetadataStore methods (if not inline)
// All methods are currently inline in the header.
// Add non-inline method stubs here if needed in the future.

} // namespace Core::Metadata
