# Training Data — Asset Descriptors

JSON descriptors for 3D assets, textures, sounds, and materials. Each entry
maps an asset path to its semantic description, tags, and generation
parameters. Used by `AssetAgent` to learn the project's visual style and to
generate new assets matching existing conventions.
