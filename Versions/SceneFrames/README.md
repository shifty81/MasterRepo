# Scene Frames

Scene-level state frames. Each file is a complete or delta serialization of
a scene (entity graph, component values, lighting, camera) at a snapshot
boundary. Used to restore or replay scene state during debugging and AI-driven
iteration.
