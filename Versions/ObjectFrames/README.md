# Object Frames

Per-object state frames. Each file records the complete serialized state of
a single tracked object (entity, asset, config) at a snapshot boundary.
Enables fast object-level history queries without scanning full scene frames.
