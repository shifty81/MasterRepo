# Experiments — Test Systems

Isolated test harnesses for validating new engine systems before integration.
Unlike unit tests, test systems exercise a full subsystem in a minimal
environment (no editor, no full engine stack). Results are documented in a
companion `RESULTS.md` file inside each sub-directory.
